def insertionSort(A, size):
    i = 0
    for i in range(size):
        j = i
        while ((j > 0) and (A[j-1] > A[j])):
            temp = A[j]
            A[j] = A[j-1]
            A[j-1] = temp
            j =j-1
